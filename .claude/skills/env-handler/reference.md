# Env Handler Reference

## Files
- `.env`: Local environment variables (ignored by git).
- `.env.example`: Template for environment variables (committed to git).
- `.env.local`: Local overrides (ignored by git).
